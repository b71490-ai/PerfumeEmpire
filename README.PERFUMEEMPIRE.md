# PerfumeEmpire

Monorepo with a .NET backend and Next.js frontend.

Structure:

- backend: ASP.NET Core Web API
- frontend: Next.js (App router)

Quick start

Backend (requires .NET 7+):

1. cd backend
2. dotnet run

Frontend (requires Node 18+):

1. cd frontend
2. npm install
3. npm run dev
